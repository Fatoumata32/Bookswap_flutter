╔══════════════════════════════════════════════════════════════╗
║  ERREUR "Error loading chats" / "Error loading listings"?   ║
║                     VOICI LA SOLUTION!                        ║
╚══════════════════════════════════════════════════════════════╝

📱 ÉTAPES (2 minutes):

1. flutter run

2. Créez un compte / Connectez-vous

3. Allez dans Settings (⚙️) > "Firestore Debug"

4. Cliquez "Run Firestore Tests"

5. Dans les LOGS, vous verrez des liens comme:
   https://console.firebase.google.com/project/.../firestore/indexes?create_composite=...

6. CLIQUEZ SUR CES LIENS
   → Ils créent automatiquement les index Firestore

7. Attendez 2-3 minutes

8. Redémarrez l'app → ✅ TOUT MARCHE!

═══════════════════════════════════════════════════════════════

POURQUOI CE PROBLÈME?

Firestore nécessite des INDEX pour les requêtes avec:
- where() + orderBy()
- arrayContains + orderBy()

Vos requêtes:
  ❌ books.where('ownerId').orderBy('createdAt')
  ❌ chats.where('participantIds').orderBy('lastMessageTime')

Sans index = ERREUR
Avec index  = ✅ FONCTIONNE

═══════════════════════════════════════════════════════════════

ALTERNATIVE (via terminal):

  firebase deploy --only firestore:indexes,firestore:rules

═══════════════════════════════════════════════════════════════

Pour plus de détails, lisez:
  - README.md
  - POURQUOI_FIRESTORE_NE_FONCTIONNE_PAS.md
  - FIREBASE_SETUP.md
